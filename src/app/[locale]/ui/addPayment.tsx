import React from 'react';

function AddPayment({ weddings }) {

    console.log(weddings[0])


    return (
        <div>
            <h2>Add Expense</h2>
            <button>Add</button>
        </div>
    );
}

export default AddPayment;